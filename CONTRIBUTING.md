# Mitwirken

Kleine Änderungen, Build, Tests und Formatierung ausführen. Neue Challenges benötigen Registry-ID, serverseitige Regel, OWASP-Quelle und Tests. Keine Dateien aus `data/` oder Secrets committen.
